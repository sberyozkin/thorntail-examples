swarm:
  deployment:
    example-keycloak.war:
      web:
        login-config:
          auth-method: KEYCLOAK-SAML
        security-constraints:
          - url-pattern: /app/secured
            methods: [GET]
            roles: [manager]
