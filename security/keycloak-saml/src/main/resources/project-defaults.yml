swarm:
  deployment:
    example-keycloak-saml.war:
      web:
        login-config:
          auth-method: KEYCLOAK-SAML
        security-constraints:
          - url-pattern: /employee-sig
            methods: [GET]
            roles: [manager]
